# GTM UAE Partner Pipeline — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core agentic framework ──────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.2.0

# ── PostgreSQL (async driver + connection pooling) ──────────────────────────
asyncpg>=0.29.0           # async driver used by the pipeline nodes
psycopg2-binary>=2.9.9    # sync driver used by the migration script

# ── Environment variable management ────────────────────────────────────────
python-dotenv>=1.0.0

# ── Data processing ─────────────────────────────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.0       # read/write .xlsx (transitional — used for data migration to PostgreSQL)

# ── HTTP client (for enrichment API calls) ──────────────────────────────────
httpx>=0.27.0          # async-friendly HTTP client for Hunter, Apollo, LinkedIn stubs

# ── Search (for LinkedIn URL discovery) ─────────────────────────────────────
tavily-python>=0.3.0   # Tavily web search API (used by linkedin_url_finder.py)
